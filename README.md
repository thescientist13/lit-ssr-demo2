# lit-ssr-demo2

To run this:

- `npm ci`
- `npm run build` (builds client and server bundles to _build/_)
- `npm run start:server`
- in another terminal, `npm run start:browser`
- Browse localhost:8080.
- Click the "Greet" button to render SSR Lit components.
